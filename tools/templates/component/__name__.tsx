import type { FC } from 'react';
import { Box, Heading } from '@chakra-ui/react';

import type { I__name__Props } from './types';

const __name__: FC<I__name__Props> = ({}) => {
  return (
    <Box>
      <Heading as='h1'>__name__(sentenceCase)</Heading>
    </Box>
  );
};

export default __name__;

__name__.displayName = '__name__';
